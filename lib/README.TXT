this folder contains the x86_64 and x86_32 library needed to use the mARC C connector. We provide static dans dynamic libraries for windows.

You need to include "gpilib.h" to your header files which use the mARC connector.
