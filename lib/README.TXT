this folder contains the x86_64 library needed to use the mARC C connector. We provide static dans dynamic libraries for Windows and Ubuntu 14.10 operating systems.

You need to include "gpilib.h" to your header files which use the mARC connector.
